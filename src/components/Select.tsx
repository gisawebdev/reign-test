import {useState} from 'react';
import {Listbox} from '@headlessui/react';

const options = ['Select your news', 'Angular', 'React', 'Vue'];

const Select = () => {
	return <div>Select</div>;
};

export default Select;
